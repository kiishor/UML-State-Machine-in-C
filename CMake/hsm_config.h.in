#ifndef HSM_CONFIG_H
	#define HSM_CONFIG_H

// Enable the hierarchical state machine
// 0 : Finite state machine
// 1 : Hierarchical state machine
#define HIERARCHICAL_STATES			${HIERARCHICAL_STATES}

#cmakedefine STATE_MACHINE_LOGGER			${STATE_MACHINE_LOGGER}

#cmakedefine01 HSM_USE_VARIABLE_LENGTH_ARRAY

#cmakedefine MAX_HIERARCHICAL_LEVEL			${MAX_HIERARCHICAL_LEVEL}

#endif // HSM_CONFIG_H